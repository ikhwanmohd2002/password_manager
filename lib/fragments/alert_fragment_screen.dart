import 'package:flutter/material.dart';

class AlertFragmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Alert Fragment Screen"),
      ),
    );
  }
}
