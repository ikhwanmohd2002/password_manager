import 'package:flutter/material.dart';

class SharedFragmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Shared Fragment Screen"),
      ),
    );
  }
}
